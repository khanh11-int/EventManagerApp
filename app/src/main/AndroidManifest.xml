<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventManagerApp">
        <activity
            android:name=".presentation.auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="false"/> <!-- MainActivity - Launcher -->
        <activity
            android:name=".presentation.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- AddEventActivity -->
        <activity
            android:name=".presentation.AddEventActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity" /> <!-- EditEventActivity -->
        <activity
            android:name=".presentation.EditEventActivity"
            android:exported="false"
            android:parentActivityName=".presentation.EventDetailActivity" /> <!-- EventDetailActivity -->
        <activity
            android:name=".presentation.EventDetailActivity"
            android:exported="false"
            android:parentActivityName=".presentation.MainActivity" /> <!-- AlarmReceiver - Broadcast Receiver -->
        <receiver
            android:name=".utils.AlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>